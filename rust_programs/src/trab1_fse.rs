use std::io;

    //Entrega 1
    //Módulo capaz de controlar a GPIO com Inputs (por polling e interrupção) / Outputs
    //(Liga/Desliga e PWM).
    //
    //Servidores distribuídos
    //1. Controlar os semáforos via GPIO com temporização
    //2. Controlar os botões de pedestre via GPIO
    //3. Medir a velocidade dos veículos via GPIO 
    //4. Reportar ao Servidor Central via TCP/IP a cada 2 segundos a contagem de veículos por sensor
    //5. Caso o veículo esteja acima de 60 km/h, enviar push ao Servidor com {cruzamento,
    //   sensor_id, velocidade_kmh, timestamp}
    //6. 

/*
Estruturas que usaremos pra representar o que tá no enunciado

struct semaforo {
  
}

struct carro {

}

*/

fn main() {

}
